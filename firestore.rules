rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra padrão: Apenas usuários autenticados podem ler ou escrever no banco de dados.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
